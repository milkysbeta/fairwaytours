name: Deploy to GitHub Pages

# A shareable preview URL at https://milkysbeta.github.io/fairwaytours/.
#
# This is for showing people the site, not for running it. GitHub Pages is
# static-only, so /api/enquiries does not exist there and the enquiry forms will
# fail — Netlify remains the real deployment. See DEPLOY.md.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Let a running deploy finish rather than cancelling it mid-publish.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Build
        # No base override. Vite emits relative asset paths and the router reads
        # its mount point off the URL, so this one artifact serves correctly at
        # https://milkysbeta.github.io/fairwaytours/ AND at the domain root.
        # Attaching fairwaytours.co.nz later is then just adding public/CNAME
        # once the DNS resolves — no rebuild config to change, and no window
        # where one URL is broken because the other was set up early.
        run: npm run build

      - name: Add SPA fallback
        # Pages has no rewrite rules, so a deep link like /enquire 404s. Serving
        # index.html as the 404 page hands the URL back to the router.
        run: cp dist/index.html dist/404.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
